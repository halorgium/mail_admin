%h1 Creating domain

= error_messages_for :domain

- form_for(:domain, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
