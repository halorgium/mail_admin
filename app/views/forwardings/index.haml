%h3
  Listing forwardings for
  = parent_name
  = link_to h(parent_object), parent_object

= render :partial => 'forwarding', :collection => current_objects

= link_to 'New forwarding', new_object_path 
